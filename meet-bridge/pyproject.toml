[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "noping-meet-bridge"
version = "0.1.0"
description = "Single-session Google Meet participant bridge for the NoBS demo"
requires-python = ">=3.11"
dependencies = ["websockets>=14,<16"]
